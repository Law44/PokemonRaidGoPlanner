<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pokeballRojo</item>
        <item name="colorPrimaryDark">@color/pokeballNegro</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MyFloatingWindow" parent="Theme.AppCompat.Dialog">
        <item name="android:textColor">@color/pokeballNegro</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@color/pokeballRojo</item>
        <item name="android:windowIsFloating">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="añadirRaid" parent="Theme.AppCompat.Dialog">
        <item name="android:background">@color/pokeballRojo</item>
        <item name="android:textColor">@color/pokeballNegro</item>
        <item name="android:textColorPrimary">@color/pokeballNegro</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:width">250dp</item>
        <item name="android:height">75dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
